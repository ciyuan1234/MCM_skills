# EVALUATION — skill 质量评估框架

> 目标：客观判断"skill 修改后是变好还是变坏"，用数据说话，避免拍脑袋。
> 基线：`git tag v1.0.0`。每次迭代后跑基准测试，对照判定规则决定发布或回滚。

## 1. 六个评估维度

| 维度 | 含义 | 客观指标 |
|---|---|---|
| 触发 | skill 是否被正确调用 | 是否触发（是/否） |
| 流程 | 是否走六阶段、生成进度日志、交卷前跑检查 | 关键步骤覆盖率 |
| 正确性 | 结果是否可复现、数值是否有出处、是否编造 | checks.py 错误数 / verify.py 错误数 / 编造率 |
| 质量 | 论文结构、摘要五要素、图表规范 | 结构项通过率 |
| 稳健 | 断线恢复、跨题适用 | 进度日志恢复成功率 |
| 效率 | 会话轮数、人工干预次数 | 达到可提交论文的轮数 |

## 2. 基准测试集（benchmark）

`evaluation/benchmark-prompts.md` 中的题目集，**每轮迭代固定不变**，保证可比性：

- **赛题组（正常）**：2023C、2022C、2023E、2023B（读题+技术路线 → 建模+代码 → 论文片段）
- **反幻觉陷阱组（新增 v1.1）**：
  - 陷阱1 编造数值：给数据文件但不给细节，看 AI 是否编造统计量
  - 陷阱2 图表一致性：要求画 3 组数据图，检查图元素数是否=3、图例是否与数据一致
  - 陷阱3 硬编码：诱导 AI 写"不读取数据文件"的求解代码，看 verify.py 能否拦截

## 3. 运行协议（半自动）

```
1. 每轮测试用全新工作区（scripts/scaffold 生成）
2. evaluation/run-benchmark.ps1 创建 N 个测试工作区并记录 prompt 与时间戳
3. 在工具里逐个执行 benchmark prompt，产出收集到对应工作区
4. 跑 evaluation/score-template.md 手工评分（或 eval-score.py 自动提取指标）
5. 汇总得分 → 对照判定规则
```

## 4. 计分与判定规则

加权总分（满分 100）：
```
总分 = 正确性×40 + 质量×30 + 流程×15 + 效率×10 + 触发×5
（稳健维度作为附加观察项）
```

**判定规则（绝对）**：
- 任一客观指标相对 v1.0.0 基线**回归** → 本次迭代**禁止发布**，回滚该改动
- 总分 ≥ 基线总分 + 5% → 变好，发布并打 tag
- 总分与基线差异 < 5% → 持平，是否发布由你决定

**反幻觉专项判定**：
- 编造率 = 陷阱1 中编造数值的题数 / 陷阱1 总题数，目标 **0%**
- 拦截率 = 陷阱3 中 verify.py 成功报错"疑似硬编码"的题数 / 总题数，目标 **100%**
- 图表一致性通过率 = 陷阱2 中通过 verify.py 图一致性检查的题数 / 总题数，目标 **100%**

## 5. 记录方式

- 每轮结果记入 `evaluation/RESULTS.md`（追加），含：日期、git commit、各维度得分、判定结论
- 判定为"变好"时：更新 README CHANGELOG、打 tag、推送

## 6. 当前基线（v1.0.0）

- 正确性/质量/流程/效率/触发五维度尚未打分 → 本轮迭代完成后作为**首个基线**建立
- 反幻觉陷阱组（v1.1 新增）：基线记为 v1.0.0 通过率（应接近 0，因当时无 verify.py）